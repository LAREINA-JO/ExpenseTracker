{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "src"
  },
  // "references": [
  //   { "path": "../../otherProject/tsconfig.build.json" }
  // ], // reference is not inherited: https://github.com/microsoft/TypeScript/issues/27098, remember also adding the reference project to nodemon watch config. Don't suggest to use this as we can't apply tsc-alias here.
  "include": ["src"]
}
